import { Box } from "@mui/material";
import React from "react";

const tabs = () => {
  return (
    <Box
        sx={{
            heigt: 400,
            width: "100%"
        }}
    >
    </Box>
  );
};

export default tabs;
